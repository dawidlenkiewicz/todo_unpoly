</div class="todo-form">
  <%= render "form" %>

  <table class="table">
    <thead>
      <tr>
        <th> Name </th>
        <th> Description </th>
      </tr>
    </thead>
    <%- @todos.each do |todo| %>
      <tbody>
        <tr>
          <td> <%= todo.name %> </td>
          <td> <%= todo.description %> </td>
        </tr>
      </tbody>
    <% end %>
  </table>
</div>
